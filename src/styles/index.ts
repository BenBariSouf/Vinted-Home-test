import { PhotoContainer, PhotoGrid } from "./StyledPhotoGrid";
import { StyledHeartIcon } from "./StyledHeartIcon";
import { StyledLoader, StyledBar } from "./StyledLoader";
export {
    PhotoContainer,
    PhotoGrid,
    StyledHeartIcon,
    StyledLoader,
    StyledBar
};

export { default as GlobalStyle } from "./GlobalStyle";